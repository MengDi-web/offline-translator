# miaomiao翻译器 · 安装与使用指南

**完全离线的中英互译工具**：词典 + 本地神经翻译双引擎，不依赖任何在线 API，不上传任何内容。

> **怎么发给你朋友**：把整个 `miaomiao翻译器-安装包` 文件夹压缩成 zip 发过去（微信/QQ/网盘均可），
> 对方按下面的步骤操作即可，**不需要懂电脑**。

---

## 🍎 macOS（苹果电脑）—— 只需 3 步

1. **解压** `miaomiao翻译器-mac.zip` → 得到 `miaomiao翻译器.app`
2. **双击** `miaomiao翻译器.app`
3. 如果提示"无法验证开发者"：**右键点应用 → 打开 → 再点打开**（只第一次需要）

之后全自动：翻译窗口打开 + 菜单栏出现「译」图标。
**不需要安装任何东西**（Node / Python / 模型全部内置在 app 里）。

**怎么用：**
- 翻译窗口里直接输入文字 → 自动翻译
- 划词：点窗口右上角「开启一键划词」→ 选中电脑上任何文字 → **Cmd+C** → 弹窗翻译
- 外观/主题：菜单栏「设置…」（有红苹果/青苹果/香蕉/汽水四套主题）

---

## 🪟 Windows —— 只需 2 步 + 每次 1 步

**第一次（约 5-15 分钟，全程不用操作）：**
1. **先解压**整个压缩包到文件夹（右键 → 全部解压；**不要**在压缩包里面直接双击，会报错）
2. 进入解压出的文件夹 → `windows\` → **双击「一键部署.cmd」**
   - 自动下载安装 Node / Python / 翻译模型，带 **SHA-256 校验**，装完自动结束

**以后每次使用：**
1. 双击 `windows\启动翻译器.bat` → **保持黑色窗口打开**
2. 打开浏览器，地址栏输入：`http://127.0.0.1:6688` → 回车

**怎么用：**
- 网页里直接输入文字 → 自动翻译
- 划词（可选）：网页上点一次「开启一键划词」→ 选中任何软件里的文字 → **Ctrl+C** → 弹窗翻译

> 黑色窗口就是翻译服务，**不要关闭**；关掉它就停止翻译了。

---

## 🔒 安全保证（对你的系统零风险）

| 项目 | 说明 |
|---|---|
| **不需要管理员权限** | macOS 不放系统目录、不装任何东西、不用输密码；Windows 的 Python 只装到**当前用户**名下，Node 是便携版放在项目文件夹里，不写注册表、不动系统文件 |
| **不联网、不上传** | 运行时服务只监听 `127.0.0.1`（本机回环地址），**不向网络暴露任何端口**；除 Windows 首次下载外全程离线，没有任何遥测/上传代码 |
| **下载有校验** | Windows 部署时 Node / Python / 模型都经过 **SHA-256 校验**，文件被篡改会立即中止并提示 |
| **开源可查证** | 全部代码开源在 GitHub（MengDi-web/offline-translator），任何懂技术的人都可以审查 |
| **卸载 = 删除文件夹** | 想卸载？直接把文件夹删掉即可，**不会在系统里留下任何残留** |

**macOS 的"无法验证开发者"提示**：这是未签名免费软件的**正常提示**（不是病毒！），按"右键 → 打开"一次即可，以后不再出现。

**Windows 首次被 SmartScreen 提示"仍要运行"**：同样正常，属未签名脚本的常规提示，选"仍要运行"。

---

## 常见问题

- **Windows 双击 bat 后浏览器打不开**：确认黑色窗口还开着；地址栏输入的是 `http://127.0.0.1:6688`
- **macOS 划词不弹窗**：先在翻译窗口点一次「开启一键划词」
- **翻译不准**：词/固定搭配走词典，句子走神经模型——都不是"专业级"，仅供参考
- **macOS 系统要求**：Apple 芯片（M1/M2/M3/M4）+ macOS 11 及以上；Intel Mac 不支持
- **Windows 系统要求**：Windows 10 / 11（64 位）
