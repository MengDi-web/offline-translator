# 一键部署（给完全不会计算机的人）

两种平台都只需"双击一下"，无需安装 Node/Python/任何东西。

## macOS（Apple 电脑）

**方式 A（推荐，免编译，完全自包含）**：把 `dist/miaomiao翻译器.app` 整个文件夹发给对方
（或压缩成 zip 发过去，对方双击解压后双击 `miaomiao翻译器.app`）。
- 应用已内置 Node、Python、翻译模型、词典 —— **无需安装任何东西**
- 首次打开如提示"无法验证开发者"，右键点应用 → 打开 → 确认即可（未签名应用的正常提示）
- 应用会自动：后台启动翻译服务 → 启动划词助手（菜单栏出现「译」）→ 打开翻译网页
- 使用：打开网页点「**开启一键划词**」→ 选中文字 → **Cmd+C** 即弹窗翻译

**方式 B（从源码构建）**：`bash tools/build-macos-app.sh`（需要本机有 Xcode 命令行工具 + Python 3.12 环境一次）

## Windows

1. 把整个项目文件夹发过去（或压缩成 zip，对方解压）
2. 让对方**双击 `windows\一键部署.cmd`**（只需一次）：
   - 自动下载便携版 Node、静默安装 Python、创建翻译环境、下载模型（需联网，约 5-15 分钟，全程无需操作）
3. 之后**双击 `windows\启动翻译器.bat`** 即可使用（划词：选中文字 → Ctrl+C）

## 备注
- 完全离线运行；翻译内容不出本机
- 首次运行下载模型/安装环境需要联网一次，之后永久离线
- 部署产物在 `dist/`（构建生成，不入 git 仓库）
