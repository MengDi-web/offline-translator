# 一键部署（给完全不会电脑的人）

## 🍎 macOS（苹果电脑）—— 一个文件搞定

**发这个文件给对方：** `dist/miaomiao翻译器-mac.zip`（520MB）

**对方只需要做 3 步：**
1. 双击解压 zip → 得到 `miaomiao翻译器.app`
2. **双击** `miaomiao翻译器.app`
3. 如果提示"无法验证开发者"：**右键点应用 → 打开 → 再点打开**（只第一次要）

之后全自动：翻译服务启动 → 划词助手启动（菜单栏出现「译」）→ 打开网页。
**不需要装 Node / Python / 任何东西**（都已内置）。

**使用：**
- 网页翻译：直接在网页输入
- 划词翻译：点网页上「开启一键划词」→ 选中文字 → **Cmd+C** → 弹窗翻译
- 弹窗外观：菜单栏「译」图标 → 设置…

**怎么发送：** 微信/QQ 传文件、或传到网盘（520MB）。

## 🪟 Windows —— 两个文件搞定

**发整个项目文件夹给对方**（压缩成 zip）：
1. 对方解压后**双击 `windows\一键部署.cmd`**（只需一次）
   - 自动下载 Node、静默安装 Python、装翻译组件、下模型（需联网，约 5-15 分钟，全程免操作）
2. 之后每次：**双击 `windows\启动翻译器.bat`** → 保持黑色窗口打开
3. 打开浏览器，**地址栏输入** `http://127.0.0.1:6688` → 回车即可翻译

**划词（可选）**：网页上点一次「开启一键划词」→ 之后任意软件里选中文字按 **Ctrl+C** 即弹窗翻译（助手随 bat 自动启动，托盘图标可退出）。

> 黑色窗口就是服务，不要关闭；关掉它就停止翻译了。

## 常见问题
- macOS 提示"无法验证开发者"：未签名应用的正常提示，按上面第 3 步操作即可
- 对方没有网：macOS 版解压后完全离线可用；Windows 版首次部署需要联网一次
- 想省 520MB 传输？也可以只把「模型」走网盘，代码走 GitHub（详见 README）
