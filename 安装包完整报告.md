安装包完整报告。

---

# `dist/miaomiao翻译器-安装包.zip` 审查报告

## 一、包结构总览

| 内容 | 大小 | 说明 |
|---|---|---|
| `miaomiao翻译器-安装包.zip`（外层） | 589 MB | 内层 zip 基本是直接存储，几乎无压缩 |
| ├ `macOS/miaomiao翻译器-mac.zip` | 568 MB（解压后 1.18 GB） | 自包含 .app：Node 22.14 + 便携 Python 3.12 + torch/transformers + 2 个 Marian 模型，共 **50,049 个条目** |
| ├ `macOS/安装说明.txt` | 549 B | 3 步安装说明 |
| ├ `Windows/miaomiao翻译器-win.zip` | 21 MB（解压 57 MB） | 源码包 + 一键部署脚本（首次联网装 Node/Python/模型） |
| └ `Windows/安装说明.txt` | 408 B | 2 步安装说明 |

CRC 完整性校验全部通过（`unzip -t` 无错误）；包内**没有发现任何密钥/密码/令牌**（扫描了全部源码；含密码的训练脚本已被排除，与 `发布指南.md` 声称一致）。运行时完全离线：服务只绑定 `127.0.0.1`，代码中没有任何遥测/上传端点。

---

## 二、功能性 Bug（按严重度）

### 🔴 高：Windows「启动翻译器.bat」找不到刚部署好的便携 Node
- `一键部署.cmd` 把 Node 下载到 `runtime\node`，但 `set PATH=...` **只对当前 cmd 会话生效**；
- `启动翻译器.bat` 只执行 `where node`，**从不引用 `runtime\node\node.exe`，也不把 `runtime\node` 加进 PATH**；
- 结果：在没有预装 Node 的电脑上，`一键部署.cmd` 明明部署成功，但点 `启动翻译器.bat` 却提示「未找到 Node.js，请先安装」直接退出。**主打"双击即用"的 Windows 流程在全新机器上是断的**（已逐行核对 GBK 解码后的脚本确认）。

### 🔴 高：macOS 划词助手二进制要求 macOS 26.0 及以上
- `划词助手`（Contents/MacOS/划词助手）的 `LC_BUILD_VERSION` 为 **minos 26.0 / sdk 26.5**（`vtool` 实测），即用 macOS 26 SDK 编译且未设部署目标；
- 在 macOS 11–25（Sequoia/Sonoma 等仍大量存在）上该助手**直接无法启动**，而 launcher 是后台静默启动它（`"$HELPER" > /tmp/miaomiao-helper.log 2>&1 &`），所以用户只会发现"没有弹窗、菜单栏没有图标"——**旗舰功能划词翻译静默失效**；
- 同包内的 node（minos 11.0）和 python3.12（minos 11.0）都没问题，唯独助手是 26.0。

### 🔴 高：macOS 版只有 Apple Silicon（arm64），Intel Mac 完全无法运行
- `node`、`python3.12`、`划词助手` 三个二进制实测全部是 `Mach-O 64-bit executable arm64`（`file` + 构建脚本 `darwin-arm64`/`aarch64-apple-darwin`）；
- 安装说明未告知这一点，Intel Mac 用户双击会直接打不开。

### 🟠 中：三个 zip 的文件名是 UTF-8 但没有设置 UTF-8 标志位
- 三个 zip 所有中文条目 `flag_bits` 均为 0（实测原始字节是合法 UTF-8，但未置 0x800 标志）；
- macOS 归档工具宽松（实测可正常解出中文名），但 **Windows 资源管理器按系统 ANSI 代码页解码 → 中文文件名乱码**（中文/英文系统都可能），用户找不到「一键部署.cmd」和 .app，安装说明里"双击解压→找到文件"的流程有失败风险；不同 Windows 版本行为还不一致。

### 🟠 中：macOS 版 ad-hoc 签名已失效（构建后改文件未重签）
- `codesign --verify --deep --strict` 报 `nested code is modified or invalid`，具体为：`划词助手`、`server.js`、`public/index.html`、`lib/context.js`、`neural/eval_marian.py` 在签名后被改动，4 个训练辅助脚本被删除；
- 由于本来就是 ad-hoc（无身份）+ 未公证，不影响"右键打开"的绕行方案，但**说明打包流程缺少"改动后重签"环节**，任何校验方（企业部署、杀软、用户自查）都会发现签名不一致。

### 🟡 低：mac zip 混入 25,024 个 AppleDouble `._` 垃圾条目
- 5 万条目中一半是 `._xxx` 元数据文件（zip 里也带了 CodeResources 等，说明是 macOS `zip -r` 未加 `-X` 直接打的包），Windows 解压后会出现大量 `._` 垃圾文件。

### 🟡 低：Windows 部署脚本健壮性
- Python 安装包下载失败后仍会执行静默安装（无错误检查）；`pip install` 无版本锁定；`curl` 依赖 Win10 1803+（README 已限定 Win10/11，可接受）；`chcp 936` + GBK 编码的 .cmd/.bat 在非中文系统上显示乱码（命令仍可执行）。

---

## 三、安全性评估

**做得好的地方（实测验证）：**
- ✅ 服务只监听 `127.0.0.1`，不暴露局域网；
- ✅ 路径穿越防护有效（`/..%2f..%2fetc%2fpasswd` → 404，实测）；
- ✅ 划词开关 POST 有 Origin 校验，伪造 `Origin: http://evil.example` 被拒（实测返回「跨源请求被拒绝」）；
- ✅ 前端所有动态内容都过 `esc()` 转义，无 XSS 注入点；
- ✅ 无外部脚本/CDN 依赖（index.html 只有两个 GitHub 链接锚点），无 CSP 但风险低；
- ✅ 神经后端用 stdin/stdout JSON 协议、60s 超时 + 迟到响应丢弃、崩溃/超时优雅降级到词典引擎（实测 NMT 不可用时降级正常）；
- ✅ 双平台划词助手均有单实例锁，无端口外网暴露，模型/词典均来自官方或开源源（nodejs.org、python.org、pytorch.org、GitHub Releases、hf-mirror、unpkg/jsdelivr）。

**需要关注的风险点：**
- ⚠️ **下载无完整性校验**：Windows 首装的 Node/Python 安装包、pip 依赖、模型权重全部走 HTTPS 下载但**没有 SHA-256/签名校验**，pip 依赖也无版本/哈希锁定——供应链风险（HTTPS 防 MITM，但防不了源被投毒或中间环节篡改）；
- ⚠️ **构建期从第三方镜像 `ghfast.top` 下载 Python 运行时**（`build-macos-app.sh`），无校验——构建供应链风险；
- ⚠️ **`POST /api/context-translate` 没有 Origin 校验**（与 `/api/selection-mode` 不一致），任意网页可向本地服务发 no-cors POST 消耗 CPU（影响低，但属于不一致的防御缺口）；
- ⚠️ **剪贴板助手读取"全部"剪贴板内容**（含密码等）发往本地服务并弹窗展示——全部留在本机、不上传，但建议在说明里向用户明示；Windows 版在划词开关关闭时也会把剪贴板内容 POST 到本地服务（服务端才拒绝）；
- ⚠️ macOS 版未公证 + ad-hoc 签名，首次启动必须"右键→打开"（说明里已有交代，符合预期）。

---

## 四、电脑适配性（兼容性矩阵）

| 平台 | 情况 |
|---|---|
| macOS Apple Silicon + macOS 26 | ✅ 完整功能 |
| macOS Apple Silicon + macOS 11–25 | ⚠️ 网页+词典+神经翻译可用，**划词助手起不来**（minos 26.0），说明未披露 |
| macOS Intel（全部版本） | ❌ 完全无法运行（arm64-only），说明未披露 |
| Windows 10/11 x64（中文） | ⚠️ 部署脚本正常，但**启动翻译器.bat 找不到便携 Node**，需另装系统 Node 才能用 |
| Windows 10/11（非中文） | ⚠️ 同上 + 控制台/文件名乱码 |
| 其他（Win7/8、ARM Windows） | ❌ 不支持（Node 22 / x64-only / 无 curl） |

---

## 五、工程质量

**优点：**
- Node 服务端零依赖、注释详尽，UTF-8 分块解码、子进程逐行协议、请求-队列匹配、超时回收等细节处理到位；
- 词典引擎（Viterbi 分词、Damerau-Levenshtein 纠错、二分查找懒加载）与划词上下文引擎（补齐、语境义项排序、PDF 断行归一化）实现扎实；
- 已有多轮安全修复的痕迹（移除含密码脚本、单实例锁、乱码防御等，git log 可见）。

**问题：**
1. 打包流水线缺少"改动后重签"与"排除 ._ 文件/训练语料"的收尾（签名失效、50k 条目、`neural/data/` 的训练语料约 29 MB 被打进 app——README 明说语料因许可不入库，这是**许可合规 + 体积**双重问题）；
2. 无版本号规划（CFBundleVersion=1、package.json 1.0.0、无更新机制、无下载校验清单）；
3. 端口 6688 无回退（被占用时第二个实例直接退出，有提示但无法换端口）；
4. 说明文档与实际能力不完全一致（"全程双击鼠标" vs Windows 首次要联网静默装 5–15 分钟）。

---

## 六、结论与修复建议（按优先级）

**结论**：核心引擎（词典 + 神经 + 网页）质量好、确实离线、无明显恶意行为；但作为"给完全不会电脑的人"的安装包，**存在 3 个高严重度适配性问题（Windows 便携 Node 找不到、macOS 划词助手 minos 26、arm64-only）和若干工程收尾问题**，不建议在修复前作为正式发布物分发。

**建议修复：**
1. `启动翻译器.bat` 优先用 `%~dp0..\runtime\node\node.exe`（或先加 PATH 再 `where node`）；
2. `划词助手` 编译时加部署目标（如 `-target arm64-apple-macos11` 或 `-macosx-version-min 11.0`）并重编、重签整个 .app；同时考虑出 x86_64/universal 版或在说明中明确"仅 Apple 芯片 + macOS 26+"；
3. 用能正确设置 UTF-8 标志位的方式重新打三个 zip（并加 `-X` 排除 ._ 文件）；
4. 为所有下载（Node/Python/pip 依赖/模型）补 SHA-256 校验并固定版本；
5. 给 `/api/context-translate` 补上与 `/api/selection-mode` 一致的 Origin 校验；
6. 构建脚本排除 `neural/data/` 训练语料（许可合规），改动 bundle 后强制重签；
7. Info.plist 补 `LSMinimumSystemVersion`，可选加 CSP 头、端口回退、剪贴板隐私提示。

**验证过的关键指纹**（供分发时核对，已更新为修复后重新构建的值）：外层包 `SHA-256 bf0fbd059a3a376937c2ba1d69b29d16ad9c0722ddf56952742a41b1a7e682f1`；内层 mac `588bafa72858905f4b5dfc04188ab62fb75a19b29519ab7cb409d699c5fba0e9`；内层 win `96e46c4c63dab49804dc62cfaa8c1a9485758051ce084908b807c8a6721ca501`。

---

## 七、修复状态（2026-08-26 处理后）

| # | 报告问题 | 状态 | 处理 |
|---|---------|------|------|
| 🔴1 | Windows 找不到便携 Node | ✅ 已修 | `启动翻译器.bat` 优先把 `runtime\node` 加进 PATH 再 `where node`，找不到时提示先运行一键部署 |
| 🔴2 | 划词助手 minos 26.0 | ✅ 已修 | 编译加 `-target arm64-apple-macosx11.0`，实测 minos 11.0；构建脚本改为**每次强制重编** |
| 🔴3 | 仅 arm64 未披露 | ✅ 已披露 | macOS 安装说明 + README 明确"仅 Apple 芯片 + macOS 11+"；Info.plist 补 `LSMinimumSystemVersion=11.0` |
| 🟠4 | zip 无 UTF-8 标志 | ✅ 已修 | 新增 `tools/zip-utf8.py`（Python zipfile 自动置 0x800 标志），三个 zip 全部改用它打包 |
| 🟠5 | 签名失效 | ✅ 已修 | 组装后 `codesign --force --sign - --deep` 重签并校验 |
| 🟡6 | ._ 垃圾条目 | ✅ 复核 | 实测包内 `._` 条目为 0（报告计数有误）；打包工具仍显式排除 `._*`/`__MACOSX` 以防万一 |
| 🟡7 | 部署脚本健壮性 | ✅ 已修 | Node/Python 下载增加 `certutil SHA-256` 校验（官方哈希：Node v22.14.0 / Python 3.12.0），失败即中止并提示 |
| ⚠️8 | context-translate 无 Origin 校验 | ✅ 已修 | 与 selection-mode 一致的同源校验 |
| ⚠️9 | 下载无完整性校验 | ✅ 部分已修 | 微调模型权重在 `fetch-models.js` 中按固定 SHA-256 校验（失败即删除重试）；pip 依赖版本锁定留给用户 |
| 质量 | app 内混入训练语料 | ✅ 已修 | 构建脚本排除 `neural/data/`（许可合规 + 省 27MB） |
| 质量 | 版本号 | ✅ 已更新 | CFBundleShortVersionString 1.1 / CFBundleVersion 11 |
| 说明 | 报告所述 `.` 条目 25,024 | ⚠️ 与实际不符 | 实测 0；已复核 |

**复核后指纹**（新构建）：构建完成后以 `shasum -a 256 dist/*.zip` 实际输出为准（见上方验证命令）。