# 发布指南（推送到 GitHub）

仓库代码已全部就绪（已 git 初始化并提交）。以下按步骤操作即可开源。

## 1. 创建 GitHub 仓库

在 https://github.com/new 创建公开仓库：
- Repository name: **offline-translator**
- Public ✓
- 不要勾选 README/.gitignore/LICENSE（本仓库已自带）

或用命令行（需安装 [gh](https://cli.github.com) 并登录）：
```bash
gh repo create offline-translator --public --source . --push
```

## 2. 推送到远程

```bash
cd ~/Documents/Ds_Harness/miaomiao翻译器
git remote add origin https://github.com/MengDi-web/offline-translator.git
git push -u origin main
```

## 3. 发布模型（GitHub Release 附件）

模型 149MB×2 超 GitHub 单文件限制，**通过 Release 附件分发**。只需上传 **2 个文件**：

```bash
# 方法 A: gh CLI
gh release create models \
  "neural/models/opus-mt-zh-en-ft/pytorch_model.bin#opus-mt-zh-en-ft-pytorch_model.bin" \
  "neural/models/opus-mt-en-zh-ft/pytorch_model.bin#opus-mt-en-zh-ft-pytorch_model.bin" \
  --title "离线翻译模型" --notes "微调权重 (zh→en / en→zh, fp16)"

# 方法 B: 网页操作
# 1) 打开仓库 → Releases → Create a new release
# 2) Tag: models, 标题: 离线翻译模型
# 3) 把这两个文件拖进附件区:
#    neural/models/opus-mt-zh-en-ft/pytorch_model.bin
#    neural/models/opus-mt-en-zh-ft/pytorch_model.bin
```

> 用户端执行 `node tools/fetch-models.js` 即会自动从
> `https://github.com/MengDi-web/offline-translator/releases/download/models/...`
> 下载这两个权重（无 Release 时自动回退 hf-mirror 预训练版，功能可用但非微调最优）。

## 4. 分发安装包（普通用户下载）

无需 git 的最终用户下载 **`dist/miaomiao翻译器-安装包.zip`**（GitHub Release 附件），
内含 `macOS/` 与 `Windows/` 两个子包 + 各平台「安装说明.txt」：

```bash
# 重新构建安装包（改代码后记得重跑）
bash tools/build-dist-package.sh
# 产物: dist/miaomiao翻译器-安装包.zip
```

- macOS 子包 = 免安装 .app（Node/Python/模型全部内置，双击即用）
- Windows 子包 = 源码 + 「一键部署.cmd」（自动装 Node/Python/模型）+ 「启动翻译器.bat」
- 模型不入 zip（macOS 已内置；Windows 首次部署时自动下载，回退 hf-mirror 也可用）

上传方式（三选一）：
```bash
gh release create v1.0 dist/miaomiao翻译器-安装包.zip --title "miaomiao翻译器 v1.0"
# 或网页 Releases → Draft a new release → 拖入 zip
# 或放到任意网盘/文件分享，发链接即可
```

## 5. 验证

```bash
git remote -v        # origin → 你的仓库
git status           # clean
```

之后在另一台机器（或新版克隆）上：
```bash
git clone https://github.com/MengDi-web/offline-translator.git
cd offline-translator
node tools/fetch-data.js && node tools/build-index.js   # 词典（仓库含 data/ 可跳过）
node tools/fetch-models.js                               # 模型
node server.js                                           # 启动
```

## 安全提示

- 已从仓库移除含服务器密码的训练辅助脚本（probe_servers.py / setup_remote.py / gpu_status.py / build_ft_models.py），不会泄露
- `翻译数据库/`（长难句语料）已包含在仓库中，如不希望公开可自行删除并提交
